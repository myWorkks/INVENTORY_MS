#
#
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_ms_db
spring.datasource.username=root
spring.datasource.password=root
#
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
#
spring.application.name=inventory-ms
server.port=0
#
#
##eureka.client.register-with-eureka=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
#
#
#


# MongoDB connection settings
#spring.data.mongodb.host=localhost
#spring.data.mongodb.port=27017
#spring.data.mongodb.database=inventory_ms_db
# Optionally, you can provide credentials if your MongoDB requires authentication
# spring.data.mongodb.username=<your_username>
# spring.data.mongodb.password=<your_password>

# Disable MySQL configurations
# spring.datasource.url=jdbc:mysql://localhost:3306/inventory_ms_db
# spring.datasource.username=root
# spring.datasource.password=root

# Use MongoDB configurations instead of MySQL
# spring.jpa.show-sql=true (This is not needed for MongoDB)
# spring.jpa.hibernate.ddl-auto=update (This is not needed for MongoDB)

#spring.application.name=inventory-ms
#server.port=0

# Eureka Client configurations
# eureka.client.register-with-eureka=true
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka
